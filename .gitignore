# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
.venv-dev/
.venv-test/
venv/

# macOS / editor
.DS_Store
.idea/
.vscode/

# Secrets and local credentials
.env
*.pem
*.key
.google_weather_usage.json
.local/

# Python build/install artifacts
*.egg-info/

# Runtime state
logs/
*.log
*.db-journal
*.sqlite
*.sqlite3
trading/data/
forecaster/logs/

# Generated training artifacts that can be rebuilt
forecaster/combined_weather.csv
forecaster/weather_features.csv
forecaster/output.csv

# Keep the copied local archive usable, but do not commit live runtime DBs by default.
forecaster/weather.db

# Large local source data is intentionally kept in WeatherEdge for learning/rebuilds.
# Remove this ignore only if you explicitly want to version raw NOAA station files.
forecaster/2016-2026 weather data/

# Dashboard runtime cache is safe to publish when intentionally updating pages,
# but should not drift into commits by accident.
forecaster/google_weather_cache.json
forecaster/trading_signal.json
forecaster/strategy_research.json
forecaster/cities_data.json
forecaster/publication_manifest.json
forecaster/details.html
forecaster/strategy-lab.html
forecaster/dataset_research.json
.gstack/

# --- Frontend (Vite/React UI) ---
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo

# live paper-trading data (fetched at runtime, published by the box)
public/strategy_research.json

# These are intentionally tracked public-dashboard fixtures, not live runtime
# authority. Keep fixture edits explicit and reviewed.
!forecaster/forecast_data.json
!forecaster/weather_story_data.json
!public/forecast_data.json
!public/weather_story_data.json

# EDA/training plot output (regenerable via eda.py and the trainers)
forecaster/plots/
